# WT-A — Server Identity (brief)

**Spec:** `docs/superpowers/specs/2026-06-09-littlelove-accounts-and-inbox-design.md`
**Branch:** `feat/v0.2-WT-A`
**Tag at end of WT verification (R1):** `v0.2.0-auth` (shared with WT-C)

## What this WT delivers

Real accounts and authenticated WSS sessions on the server. By the end of this WT:
- `POST /accounts` creates an account from a username + Ed25519 pub + X25519 pub.
- `GET /accounts/by-username/{username}` returns those publics.
- WSS handshake replaces the trusted `x-llove-user` header with Challenge → Identify → Authenticated.
- A client signing with the wrong key gets close code 4001.
- The `Send` frame's `from` field is overridden server-side with the authenticated identity (no spoofing).

## Spec sections you MUST read

- §3 (Identity & Auth) — esp. §3.3 (Connecting to the server).
- §8.1 — `POST /accounts`, `GET /accounts/by-username/{username}`.
- §8.2 — `Challenge`, `Identify`, `Authenticated`, `Send`, `Message`, `Error` frames.
- §8.3 — Auth surface (WSS-only).
- §8.4 — Migration `0002_accounts.sql`.
- §8.5 — Cryptographic primitives (use the exact crates).
- §9 — Server module layout (your additions: `auth.rs`, `accounts.rs`; your modifications: `ws.rs`).
- §11 — Testing strategy row for WT-A.
- §12.1 — Files owned + dependency on spec sections.
- §13 — Acceptance criteria #1 (partial — server side) and #6.

## Files you own

| Action | Path |
|---|---|
| create | `server/src/auth.rs` |
| create | `server/src/accounts.rs` |
| modify | `server/src/ws.rs` |
| modify | `server/src/main.rs` (route registration) |
| create | `server/migrations/0002_accounts.sql` |
| modify | `server/Cargo.toml` (add `ed25519-dalek`, `rand`) |
| create | `server/tests/accounts_create.rs` |
| create | `server/tests/accounts_lookup.rs` |
| create | `server/tests/auth_handshake.rs` |

## Files you MUST NOT touch (other WTs own them)

- `server/src/invites.rs` (WT-B)
- `server/src/rooms.rs` (WT-B)
- `server/migrations/0003*`, `0004*`, `0005*` (WT-B)
- Anything under `app/` (WT-C/D/E)

## Acceptance criteria

You are done when:

1. Spec AC #1 server-side passes: a client can `POST /accounts`, then complete a `Challenge → Identify → Authenticated` flow over WSS. Wrong signature → close 4001.
2. Spec AC #6 passes: `x-llove-user` header reads are deleted; `Send` frames inbound have `from` rewritten to the authenticated account; tests prove a forged `from` is overridden.
3. `cargo fmt --check`, `cargo clippy -- -D warnings`, `cargo test -p server` all green.
4. Integration test `server/tests/auth_handshake.rs` covers: success path, bad signature, unknown username, replay attack (server's nonce is single-use per connection).

## TDD ordering (suggested first 10 steps for `writing-plans`)

1. Write failing migration test: `accounts` table exists with required columns + unique index on `username`.
2. Implement migration `0002_accounts.sql` → green.
3. Write failing `accounts_create.rs`: POST /accounts with valid body → 201; duplicate username → 409.
4. Implement `accounts.rs::create_account` and route registration → green.
5. Write failing `accounts_lookup.rs`: GET /accounts/by-username for unknown → 404; known → 200 with publics.
6. Implement `accounts.rs::get_account` → green.
7. Write failing `auth.rs::random_nonce` test: returns 32 bytes, never repeats across N=10000 calls.
8. Implement `random_nonce` using `rand::rngs::OsRng` → green.
9. Write failing `auth.rs::verify_signature` test: valid sig over nonce returns Ok; tampered nonce/sig → Err.
10. Implement using `ed25519-dalek` → green.

Continue from there with WSS handshake integration tests. The plan you write should keep each step at ~2-5 minutes.

## Crypto primitives (from spec §8.5 — use these exact crates, no substitutions)

- `ed25519-dalek = "2"` for signature verify.
- `rand_core` + `rand` for OsRng-backed nonce generation.
- `base64` for wire encoding.
- (X25519 lib unused in WT-A — that's WT-D's client-side concern. Server only stores the X25519 pub bytes opaquely.)

## What to do at the end

1. Run the full test suite + lints locally.
2. Confirm AC #1 server-side and AC #6 mechanically.
3. `gh pr create` against `main` with a body that links the spec sections it implements.
4. Wait for R1 verification (Court will run R1 once WT-C also lands).

## Stop conditions

Stop and ping Court if you find:
- A spec ambiguity in §3, §8.1, §8.2, §8.3, §8.4, or §8.5.
- A required Rust crate version is yanked or has a known CVE.
- An acceptance criterion in §13 isn't expressible as a test.

Don't push through ambiguity; amend the spec in `main` first (see spec §12.4).
