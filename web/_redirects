# Universal-link web fallback: any /pair/<code> serves the static fallback page
# (200 rewrite, not a redirect — the URL stays /pair/<code> so iOS can still
# intercept it as a universal link when the app IS installed). The token is
# never consumed in the browser.
/pair/*  /pair/  200
