# Rust
/target/
Cargo.lock

# Flutter
/app/.dart_tool/
/app/.flutter-plugins
/app/.flutter-plugins-dependencies
/app/.packages
/app/.pub-cache/
/app/.pub/
/app/build/
/build/
/app/littlelove.iml
/app/macos/Pods/
/app/macos/Podfile.lock
/app/windows/x64/

# Dev scripts (per-worktree, generated)
.dev.env

# Superpowers brainstorming session content
.superpowers/

# Claude Code agent state + git worktrees (.claude/worktrees can be many GB)
.claude/

# Flutter / Dart per-user tool state (created when HOME is rerooted into the repo,
# e.g. demo.sh sandboxing kaitlyn's identity). Never commit these.
app/.config/
app/.flutter
app/Library/
.dev.demo.key
/.dev/

# OS
.DS_Store
Thumbs.db

# Editor
.idea/
.vscode/
*.iml
